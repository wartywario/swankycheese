# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project purpose

A static website being built to replace a Squarespace site. Currently in early setup — the repo contains only `index.html` and a `README.md`.

## Development

No build tooling is configured yet. Open `index.html` directly in a browser to preview, or use any static file server (e.g. `npx serve .` or VS Code Live Server).

## Remote

GitHub: https://github.com/wartywario/swankycheese
